public class Resource {
    double velocity; // скорость транспорта, (км/ч)
    double capacity; // вместимость одного грузового автомобиля

    public Resource(double velocity, double capacity){
        this.velocity = velocity;
        this.capacity = capacity;
    }
}
